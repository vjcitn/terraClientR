# FireCloud
#
# FireCloud API 
#
# The version of the OpenAPI document: 0.1
# 
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title Workflow
#'
#' @description Workflow Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field workflowId  character [optional]
#'
#' @field status  \link{WorkflowStatus} [optional]
#'
#' @field statusLastChangedDate  character [optional]
#'
#' @field workflowEntity  \link{AttributeEntityReference} [optional]
#'
#' @field inputResolutions  list( \link{SubmissionValidationValue} ) [optional]
#'
#' @field messages  list( character ) [optional]
#'
#' @field cost  numeric [optional]
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
Workflow <- R6::R6Class(
  'Workflow',
  public = list(
    `workflowId` = NULL,
    `status` = NULL,
    `statusLastChangedDate` = NULL,
    `workflowEntity` = NULL,
    `inputResolutions` = NULL,
    `messages` = NULL,
    `cost` = NULL,
    initialize = function(
        `workflowId`=NULL, `status`=NULL, `statusLastChangedDate`=NULL, `workflowEntity`=NULL, `inputResolutions`=NULL, `messages`=NULL, `cost`=NULL, ...
    ) {
      local.optional.var <- list(...)
      if (!is.null(`workflowId`)) {
        stopifnot(is.character(`workflowId`), length(`workflowId`) == 1)
        self$`workflowId` <- `workflowId`
      }
      if (!is.null(`status`)) {
        stopifnot(R6::is.R6(`status`))
        self$`status` <- `status`
      }
      if (!is.null(`statusLastChangedDate`)) {
        stopifnot(is.character(`statusLastChangedDate`), length(`statusLastChangedDate`) == 1)
        self$`statusLastChangedDate` <- `statusLastChangedDate`
      }
      if (!is.null(`workflowEntity`)) {
        stopifnot(R6::is.R6(`workflowEntity`))
        self$`workflowEntity` <- `workflowEntity`
      }
      if (!is.null(`inputResolutions`)) {
        stopifnot(is.vector(`inputResolutions`), length(`inputResolutions`) != 0)
        sapply(`inputResolutions`, function(x) stopifnot(R6::is.R6(x)))
        self$`inputResolutions` <- `inputResolutions`
      }
      if (!is.null(`messages`)) {
        stopifnot(is.vector(`messages`), length(`messages`) != 0)
        sapply(`messages`, function(x) stopifnot(is.character(x)))
        self$`messages` <- `messages`
      }
      if (!is.null(`cost`)) {
        stopifnot(is.numeric(`cost`), length(`cost`) == 1)
        self$`cost` <- `cost`
      }
    },
    toJSON = function() {
      WorkflowObject <- list()
      if (!is.null(self$`workflowId`)) {
        WorkflowObject[['workflowId']] <-
          self$`workflowId`
      }
      if (!is.null(self$`status`)) {
        WorkflowObject[['status']] <-
          self$`status`$toJSON()
      }
      if (!is.null(self$`statusLastChangedDate`)) {
        WorkflowObject[['statusLastChangedDate']] <-
          self$`statusLastChangedDate`
      }
      if (!is.null(self$`workflowEntity`)) {
        WorkflowObject[['workflowEntity']] <-
          self$`workflowEntity`$toJSON()
      }
      if (!is.null(self$`inputResolutions`)) {
        WorkflowObject[['inputResolutions']] <-
          lapply(self$`inputResolutions`, function(x) x$toJSON())
      }
      if (!is.null(self$`messages`)) {
        WorkflowObject[['messages']] <-
          self$`messages`
      }
      if (!is.null(self$`cost`)) {
        WorkflowObject[['cost']] <-
          self$`cost`
      }

      WorkflowObject
    },
    fromJSON = function(WorkflowJson) {
      WorkflowObject <- jsonlite::fromJSON(WorkflowJson)
      if (!is.null(WorkflowObject$`workflowId`)) {
        self$`workflowId` <- WorkflowObject$`workflowId`
      }
      if (!is.null(WorkflowObject$`status`)) {
        statusObject <- WorkflowStatus$new()
        statusObject$fromJSON(jsonlite::toJSON(WorkflowObject$status, auto_unbox = TRUE, digits = NA))
        self$`status` <- statusObject
      }
      if (!is.null(WorkflowObject$`statusLastChangedDate`)) {
        self$`statusLastChangedDate` <- WorkflowObject$`statusLastChangedDate`
      }
      if (!is.null(WorkflowObject$`workflowEntity`)) {
        workflowEntityObject <- AttributeEntityReference$new()
        workflowEntityObject$fromJSON(jsonlite::toJSON(WorkflowObject$workflowEntity, auto_unbox = TRUE, digits = NA))
        self$`workflowEntity` <- workflowEntityObject
      }
      if (!is.null(WorkflowObject$`inputResolutions`)) {
        self$`inputResolutions` <- ApiClient$new()$deserializeObj(WorkflowObject$`inputResolutions`, "array[SubmissionValidationValue]", loadNamespace("openapi"))
      }
      if (!is.null(WorkflowObject$`messages`)) {
        self$`messages` <- ApiClient$new()$deserializeObj(WorkflowObject$`messages`, "array[character]", loadNamespace("openapi"))
      }
      if (!is.null(WorkflowObject$`cost`)) {
        self$`cost` <- WorkflowObject$`cost`
      }
      self
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`workflowId`)) {
        sprintf(
        '"workflowId":
          "%s"
                ',
        self$`workflowId`
        )},
        if (!is.null(self$`status`)) {
        sprintf(
        '"status":
        %s
        ',
        jsonlite::toJSON(self$`status`$toJSON(), auto_unbox=TRUE, digits = NA)
        )},
        if (!is.null(self$`statusLastChangedDate`)) {
        sprintf(
        '"statusLastChangedDate":
          "%s"
                ',
        self$`statusLastChangedDate`
        )},
        if (!is.null(self$`workflowEntity`)) {
        sprintf(
        '"workflowEntity":
        %s
        ',
        jsonlite::toJSON(self$`workflowEntity`$toJSON(), auto_unbox=TRUE, digits = NA)
        )},
        if (!is.null(self$`inputResolutions`)) {
        sprintf(
        '"inputResolutions":
        [%s]
',
        paste(sapply(self$`inputResolutions`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`messages`)) {
        sprintf(
        '"messages":
           [%s]
        ',
        paste(unlist(lapply(self$`messages`, function(x) paste0('"', x, '"'))), collapse=",")
        )},
        if (!is.null(self$`cost`)) {
        sprintf(
        '"cost":
          %d
                ',
        self$`cost`
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(WorkflowJson) {
      WorkflowObject <- jsonlite::fromJSON(WorkflowJson)
      self$`workflowId` <- WorkflowObject$`workflowId`
      self$`status` <- WorkflowStatus$new()$fromJSON(jsonlite::toJSON(WorkflowObject$status, auto_unbox = TRUE, digits = NA))
      self$`statusLastChangedDate` <- WorkflowObject$`statusLastChangedDate`
      self$`workflowEntity` <- AttributeEntityReference$new()$fromJSON(jsonlite::toJSON(WorkflowObject$workflowEntity, auto_unbox = TRUE, digits = NA))
      self$`inputResolutions` <- ApiClient$new()$deserializeObj(WorkflowObject$`inputResolutions`, "array[SubmissionValidationValue]", loadNamespace("openapi"))
      self$`messages` <- ApiClient$new()$deserializeObj(WorkflowObject$`messages`, "array[character]", loadNamespace("openapi"))
      self$`cost` <- WorkflowObject$`cost`
      self
    }
  )
)

